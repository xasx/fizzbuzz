apply plugin: 'java'
apply plugin: 'application'

mainClassName = "pkgname.FizzBuzz"

repositories {
    jcenter()
}

dependencies {

    compile 'com.google.guava:guava:18.0'

//    testCompile "junit:junit:4.11"
}

jar {
    manifest {
        attributes 'Main-Class': 'pkgname.FizzBuzz'
    }
}
